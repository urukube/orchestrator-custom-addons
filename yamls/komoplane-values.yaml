# =============================================================================
# Komoplane Helm Chart Values
# Chart: https://github.com/komodorio/komoplane/tree/main/charts/komoplane
# =============================================================================
# Komoplane serves its UI at / with no configurable base path.
# Path prefix stripping is handled by the Istio VirtualService rewrite.

service:
  type: ClusterIP
  port: 8090
